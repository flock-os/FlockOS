# FlockOS — AS-BUILT

> Complete system inventory as of **March 31, 2026 — v1.3**

A complete church management platform built on Google Apps Script and Google Sheets. One unified API endpoint, 200 spreadsheet tabs, RBAC authentication, multi-church deployment system, and a comprehensive deployment guide — everything needed to run a church CRM without a traditional server.

> *"I am the vine; you are the branches."* — John 15:5

---

## System Summary

| Metric | Value |
|--------|-------|
| **Version** | 1.3 |
| **JavaScript source** | 21 files — 39,770 lines (FlockOS/Scripts/) |
| **HTML pages** | 13 files — 7,944 lines (FlockOS/Pages/) |
| **Image assets** | 6 files — FlockOS_Blue/Green/Midnight/Orange/Pink/White.png (FlockOS/Images/) |
| **Backend source** | 1 unified `.gs` file — ~25,000 lines (Tools/Current Deployment/Single.gs) |
| **Active deployments** | 3 — FlockOS (default), Trinity Baptist Church, Test |
| **Church configs** | 4 JSON files (FlockOS/Tools/Active Deployments/) |

---

## Architecture

FlockOS uses a single Google Sheet with 200 tabs as its database, exposed through one Google Apps Script Web App (`Single.gs`, ~25,000 lines). The backend is organized into four functional domains:

| Domain | Codename | Tabs | Purpose |
|--------|----------|------|---------|
| **FLOCK** | John | 79 | Members, auth, pastoral care, services, songs, communications |
| **EXTRA** | Luke | 53 | Analytics, metrics, future expansion slots |
| **APP** | Matthew | 12 | Public content (devotionals, lexicon, quiz, theology) |
| **MISSIONS** | Mark | 56 | Persecution data, country dossiers, mission teams |

**Total: 200 tabs in 1 Google Sheet, 1 GAS project, 1 Web App endpoint**

The `.gs` backend is deployed directly via the Apps Script editor — it is not committed to git. A reference copy lives in `FlockOS/Tools/Current Deployment/Single.gs` for local documentation and schema work.

---

## Folder Structure (AS-BUILT)

```
FlockOS-SF/
│
├── index.html                           Public-facing site — auth-scoped navigation
├── manifest.json                        PWA manifest
├── the_living_water.js                  Service worker
├── LICENSE                              Proprietary CRM + Free Learning Modules
└── README.md                            This file
│
├── Church/                              ← Built church deployments (generated by build_churches.sh)
│   ├── FlockOS/                         Default deployment
│   │   ├── index.html
│   │   ├── manifest.json
│   │   ├── the_living_water.js
│   │   └── FlockOS/                     Branded copy of source
│   ├── TBC/                             Trinity Baptist Church deployment
│   │   └── ...                          (same structure)
│   └── Test/                            Test deployment
│       └── ...                          (same structure)
│
└── FlockOS/                             ← SOURCE (single codebase)
    │
    ├── Images/                          Logo and image assets
    │   ├── FlockOS_Blue.png             Logo (blue — TBC)
    │   ├── FlockOS_Green.png            Logo (green)
    │   ├── FlockOS_Midnight.png         Logo (midnight — default)
    │   ├── FlockOS_Orange.png           Logo (orange)
    │   ├── FlockOS_Pink.png             Logo (pink)
    │   └── FlockOS_White.png            Logo (white)
    │
    ├── Pages/                           Production HTML pages (served via GitHub Pages)
    │   ├── the_pentecost.html           1,472 lines — Interactive deployment guide
    │   ├── the_good_shepherd.html       1,360 lines — Admin dashboard
    │   ├── index.html                     716 lines — Main app shell source
    │   ├── fishing-for-men.html           699 lines — Value proposition document
    │   ├── the_gift_drift.html            527 lines — Spiritual Gifts 4-phase curriculum
    │   ├── the_wall.html                  512 lines — Login page
    │   ├── the_anatomy_of_worship.html    493 lines — Anatomy of Worship standalone page
    │   ├── the_call_to_forgive.html       480 lines — Call to Forgive standalone page
    │   ├── the_generations.html           460 lines — Generations of Faith standalone page
    │   ├── the_weavers_plan.html          408 lines — Weaver's Plan 8-chapter curriculum
    │   ├── prayerful_action.html          387 lines — Prayerful Action standalone page
    │   ├── the_invitation.html            255 lines — Invitation page
    │   └── Learn More.html                175 lines — Learn More template
    │
    ├── Scripts/                          Production JavaScript (served via GitHub Pages)
    │   ├── the_tabernacle.js           15,620 lines — 48+ module renderers, Themes, Interface Studio
    │   ├── fine_linen.js                5,438 lines — CSS theme system (13 themes) + Interface Studio
    │   ├── the_way.js                   3,361 lines — Learning Hub — 16-tab education dashboard
    │   ├── the_life.js                  2,404 lines — My Flock Portal — pastoral command hub
    │   ├── the_seasons.js               2,360 lines — Calendar, Tasks & Check-In Hub
    │   ├── the_shofar.js                1,621 lines — Song library, chord charts, PDF export
    │   ├── the_commission.js            1,335 lines — Deployment automation blueprint
    │   ├── the_true_vine.js             1,307 lines — Centralized API client (unified endpoint)
    │   ├── the_harvest.js                 934 lines — Ministry Hub — events, sermons, services, songs
    │   ├── the_shepherd.js                866 lines — People Engine — member search/profile/3-step save
    │   ├── the_cornerstone.js             790 lines — Architecture registry (runtime-queryable)
    │   ├── the_wellspring.js              752 lines — Local data layer — IndexedDB
    │   ├── the_well.js                    559 lines — Google Drive sync for offline churches
    │   ├── love_in_action.js              523 lines — Care Hub — 4-tab care/prayer/compassion/outreach
    │   ├── the_trumpet.js                 486 lines — Notification system (admin push alerts)
    │   ├── firm_foundation.js             462 lines — Authentication guard
    │   ├── the_scrolls.js                 307 lines — Interaction Ledger — 30+ pastoral touchpoint types
    │   ├── the_fold.js                    288 lines — Groups & Attendance — 2-tab interface
    │   ├── the_living_water.js            197 lines — Service worker (source)
    │   ├── the_pagans.js                  160 lines — Public-facing content helpers
    │   └── the_truth.js                     0 lines — Reserved placeholder (John 8:32)
    │
    └── Tools/                           Backend source, deployment reference & build system
        ├── Active Deployments/          Church configuration files
        │   ├── ChurchTemplate.json      Blank template for new churches
        │   ├── flockos-default.json     FlockOS default deployment config
        │   ├── flockos-tbc.json         Trinity Baptist Church config
        │   └── flockos-test.json        Test deployment config
        ├── Current Deployment/          Backend source & documentation
        │   ├── Single.gs               ~25,000 lines — Unified backend (paste as Code.gs)
        │   ├── Backup_Single.gs         Backup / bidirectional sync system
        │   ├── Deployment Guide.md      Step-by-step deployment guide
        │   ├── Action Items.md          Development action items & gaps
        │   ├── Master Schema Audit.md   Schema vs. code audit
        │   ├── API Code.md              Complete API reference
        │   ├── Database Backup Manager.md  Backup system documentation
        │   ├── Master Git Ignore.md     Git ignore rules
        │   └── Single File Application.md  Single-sheet feasibility analysis
        ├── Development Scripts/         Build & automation tools
        │   ├── build_churches.sh        Multi-church build script
        │   ├── build.sh                 Backend deployment script
        │   ├── flockos_project_notes.md Consolidated project notes
        │   ├── minify.py / minify.sh    Minification tools
        │   ├── remove_minified.py       Clean minified files
        │   ├── rename_min.py            Rename minified files
        │   └── test_regex.py            Regex testing utility
        └── Master Deployment/
            └── Instructions for Master.md  Multi-church deployment instructions
```

---

## Git-Tracked vs. Local-Only

| Scope | Folder / Files | Notes |
|-------|---------------|-------|
| Root | `index.html`, `manifest.json`, `the_living_water.js`, `LICENSE`, `README.md` | Public site + PWA manifest + service worker |
| Images | 6 `.png` files (Blue/Green/Midnight/Orange/Pink/White) | Logo assets for all deployments |
| Pages | 13 `.html` files | Production HTML (served directly via GitHub Pages) |
| Scripts | 21 `.js` files | Production JavaScript (served directly via GitHub Pages) |
| Church/ | 3 deployment folders (FlockOS, TBC, Test) | Generated by `build_churches.sh` — do not edit directly |
| Active Deployments | 4 `.json` config files + template | Church-specific config (name, logo, API URL, branding) |
| Tools/Current Deployment | `Single.gs`, `Backup_Single.gs`, deployment docs | Backend source & deployment reference |

---

## Quick Start

Full step-by-step instructions are in **the_pentecost.html** (`FlockOS/Pages/`) — open it in a browser for the best experience. The short version:

### 1. Create Google Sheet
Create one Google Sheet named **FlockOS — [Church Name]**. Copy the Sheet ID from the URL.

### 2. Deploy the API
1. Create a new Apps Script project at [script.google.com](https://script.google.com)
2. Paste the contents of `Tools/Current Deployment/Single.gs` (~25,000 lines) as `Code.gs`
3. Set Script Property: `SHEET_ID` = your FlockOS Sheet ID
4. Run `setupFlockOS()` → creates all 200 tabs
5. Run `initPepper()` → generates encryption secret
6. Seed your admin user in AuthUsers + AccessControl tabs
7. Run `migrateAllPasswords()` → hashes the password
8. Deploy as Web App (Execute as: Me, Access: Anyone)

### 3. Configure the church deployment
1. Copy `FlockOS/Tools/Active Deployments/ChurchTemplate.json` → `flockos-yourchurch.json`
2. Fill in: `id`, `name`, `shortName`, `brandName`, `logo`, `databaseUrl`, `adminEmail`
3. Run `bash FlockOS/Tools/Development\ Scripts/build_churches.sh`
4. Commit and push — church is live at `Church/<shortName>/`

---

## Authentication

- **Method**: Email + passcode
- **Hashing**: SHA-256 with per-user salt + server-side pepper
- **Sessions**: 6-hour TTL, stored client-side
- **RBAC**: 6 levels — readonly (0), volunteer (1), care (2), leader (3), pastor (4), admin (5)

---

## Key Features

- **Member Management** — Full CRUD with pastoral notes, tags, contact masking
- **Member Cards** — Sequential card numbers with configurable prefix (default ATOG-0001)
- **Member Name Dropdowns** — All assignment fields sitewide use member directory select dropdowns (Care, Compassion, Messages, Giving, Prayer)
- **Module Visibility** — Admin toggle switches to show/hide any module from public & admin navigation
- **Tasks Module** — Card grid with filters (status/priority/category), complete/archive/delete, recurring tasks, priority badges, category pills
- **Prayer Admin** — Card grid layout with full prayer text prominent, metadata as pills (status, category, confidential, follow-up, assigned, date), Reply per card
- **Prayer Request** — Public prayer submission + admin management with status, assignment & replies
- **Messages** — Card-based inbox/sent with avatar initials, time-ago dates, unread indicators. Full message detail view with read receipts, reply with quoted original, forward, SMS via native link, delete with confirmation
- **Notifications** — Topbar bell with unread count badge, dropdown panel with subject/body/time-ago, dismiss or mark-all-read, 30s polling, click-to-navigate routing for 20+ module types, mobile-responsive full-width panel
- **Journaling** — Personal journal for prayer, study & reflection (readonly+ access) with scripture linking
- **Bible.com Integration** — Scripture references link directly to bible.com ESV across all modules
- **Daily Devotional** — Unified daily spiritual life page: single-card date navigation, devotion (Scripture/Reflection/Question/Prayer), reading plan (OT/NT/Ps/Pr), inline journal entry (members only), and prayer request submission — all in one view
- **Control Panel (Admin Settings)** — All 8 sections are collapsible accordion panels (all closed by default) with summary counts/status in the trigger bar: Security, Identity & Branding, Display Preferences, Theme Administration, Visible Apps, System Settings, API Health, and Provisioning
- **Themes Module (Public)** — Dedicated module accessible to all users via Growth nav group. Personal theme picker with swatch grid (respects admin's Allow Custom Themes toggle). Full Interface Studio: 30+ font-size sliders, padding/spacing controls (including split V/H pairs), corner-radius sliders, shadow-intensity dropdown, body & heading font pickers (18 Google Fonts), custom CSS textarea. Live preview on every change; Save All persists to localStorage + backend AppConfig; Reset to Defaults clears everything; Restore Visuals to Default reverts unsaved changes
- **Theme Administration (Admin)** — In Control Panel: foundational theme dropdown + swatch row, Allow Custom Themes toggle. When disabled, personal themes and Interface Studio are locked for all users
- **API Health Dashboard** — In Control Panel: color-coded health pill (green/yellow/red) for the unified API endpoint. Run Diagnostics button calls `TheVine.manifest.diagnostics()` and shows latency. Pill also appears on the Provisioning card
- **Health Pills** — CSS component (`.health-pill-ok/warn/fail/unknown`) in fine_linen.js with animated pulsing dots, wired to the theme's `--success/--warning/--danger` CSS variables
- **Dual Font Scale** — Separate desktop and mobile font-size sliders in Control Panel. Viewport-aware IIFE with `matchMedia` applies the correct scale
- **Provisioning** — Single Database URL field with monospace `.prov-url-field` styling. `_resolveUrl()` resolves the endpoint; `_appTab()` handles failover. Test Endpoint pings the health endpoint. Saved to localStorage + backend AppConfig and auto-loaded on init via `TheVine.configure()`
- **Audit Logging** — All flock and prayer interactions are tracked with user, action, and timestamp
- **Service Planning** — Service order builder with item scheduling
- **Songs & Music Stand** — Song catalog, ChordPro chord charts, arrangement management, live setlist view, PDF lead-sheet export
- **Spiritual Care** — Case tracking, interaction logging, follow-up management, member name assignment
- **Communications** — Email/SMS templates, campaign tracking, delivery logs
- **Compassion & Outreach** — Benevolence fund, needs tracking, outreach campaigns, member name assignment
- **Discipleship & Learning** — Growth paths, milestones, courses, quizzes
- **World Missions** — 48 country dossiers, partner tracking, team management
- **Volunteers** — Schedule & team assignments, member directory name lookup, vCard download links
- **Statistics** — Analytics dashboards, attendance, giving, growth metrics
- **Multi-Church** — Campus management, shared resources (expansion-ready)
- **iOS Mobile** — All inputs 16px to prevent Safari auto-zoom, lightened input backgrounds, responsive modals, root font-size: 100% for user font scaling
- **Local Data Mode (TheWellspring)** — Offline-capable data layer for churches without reliable internet. Import a single .xls/.xlsx file of the full FlockOS database via the browser, stored in IndexedDB, served through TheVine’s resolver hook. Toggle, import, export, and status controls in Settings Data Source panel. Cloud-hosted interface remains unchanged; only the data source changes
- **Google Drive Sync (TheWell)** — Connects to Google Drive so church data stored as .xlsx files can sync automatically. Built on top of TheWellspring. OAuth 2.0 browser-only flow, configurable auto-sync interval, push local changes back to Drive
- **Learning Hub (TheWay)** — Consolidated growth portal: 16 tabs covering courses, quizzes, reading plans, theology, lexicon, apologetics, counseling, devotionals, certificates, and analytics. Dashboard with KPIs, streak tracking, continue-learning, recommendations. Standalone HTML curriculum pages (Gift Drift, Weaver's Plan, Generations, Anatomy of Worship, Prayerful Action) linked from scrollable card row with smart `?from=public|admin` back-link routing. Course quizzes sample 12 random questions from the pool
- **Heart Check** — Self-assessment diagnostic in Learning Hub and module renderers. Yes = danger (red), No = healthy (green). Results logged silently to localStorage for pastoral context
- **Silent Diagnostic Logging** — Quiz scores and Heart Check results are stored in `localStorage` under `flockos_diagnostics`. When a user submits a prayer request, the most recent diagnostic snapshot is automatically appended as a `--- Spiritual Context (auto) ---` block, giving pastors insight without requiring the user to self-report
- **Ministry Hub (TheHarvest)** — Events, sermons, service plans, songs, ministry teams, and volunteer scheduling consolidated into one dashboard. KPI ribbon, upcoming events timeline, donut charts, quick actions
- **Calendar Hub (TheSeasons)** — Unified calendar with month/week/day/agenda views, personal events, iCal feeds, recurrence engine, task management, and attendance check-in. 7-tier role-based event visibility
- **My Flock Portal (TheLife)** — Pastoral command hub with dashboard-of-apps pattern: People, Love in Action, The Fold, Activity. Full-page editors for members, care cases, prayer, compassion, outreach
- **Admin Dashboard** — KPI ribbon, scratchpad, quick tasks, font size controls (75–150%), quick action links
- **Tools Launchpad** — Card grid of 27 app launchers covering all major modules
- **Auth-Scoped Navigation** — Public site sidebar uses `data-auth` attributes to show/hide nav items based on authentication status: public (always visible), required (logged-in users), admin (admin role only)
- **Per-User Module Permissions** — GRANT/DENY overrides per user via Permissions sheet. `Nehemiah.canAccess(moduleKey)` for frontend gating, `requireModule()` for API enforcement

---

## Frontend File Map

Each source file in `FlockOS/Scripts/` has a specific role in the frontend architecture:

| Source (Scripts/) | Lines | Role |
|-------------------|------:|------|
| `the_tabernacle.js` | 15,620 | Core renderer — 48+ module UIs, Themes, Interface Studio |
| `fine_linen.js` | 5,438 | CSS theme system (13 themes) + Interface Studio styles |
| `the_way.js` | 3,361 | Learning Hub — 16-tab education dashboard |
| `the_life.js` | 2,404 | My Flock Portal — pastoral command hub |
| `the_seasons.js` | 2,360 | Calendar, Tasks & Check-In Hub |
| `the_shofar.js` | 1,621 | Song library, chord charts, Music Stand, PDF export |
| `the_commission.js` | 1,335 | Deployment automation blueprint |
| `the_true_vine.js` | 1,307 | Centralized API client (unified endpoint) |
| `the_harvest.js` | 934 | Ministry Hub — events, sermons, services, songs |
| `the_shepherd.js` | 866 | People Engine — member search/profile/3-step save |
| `the_cornerstone.js` | 790 | Architecture registry (runtime-queryable) |
| `the_wellspring.js` | 752 | Local data layer — single-file .xls/.xlsx import via IndexedDB |
| `the_well.js` | 559 | Google Drive sync for offline churches |
| `love_in_action.js` | 523 | Care Hub — 4-tab care/prayer/compassion/outreach |
| `the_trumpet.js` | 486 | Notification system (admin push alerts) |
| `firm_foundation.js` | 462 | Authentication guard |
| `the_scrolls.js` | 307 | Interaction Ledger — 30+ pastoral touchpoint types |
| `the_fold.js` | 288 | Groups & Attendance — 2-tab interface |
| `the_living_water.js` | 197 | Service worker (source) |
| `the_pagans.js` | 160 | Public-facing content helpers |
| `the_truth.js` | 0 | Reserved placeholder (John 8:32) |

---

## Backend Reference Files (Tools/Current Deployment/)

The `.gs` backend is deployed directly via the Apps Script editor. The unified `Single.gs` file is the production backend:

| File | Purpose |
|------|--------|
| `Single.gs` | **Unified backend** (~25,000 lines) — all four domains (Flock, Extra, App, Missions) |
| `Backup_Single.gs` | Bidirectional backup & sync system with onChange/time-based triggers |
| `Deployment Guide.md` | Step-by-step deployment guide (primary, backup, peer registration, sync) |
| `Action Items.md` | Development action items & backend/frontend gaps |
| `API Code.md` | Complete API reference — all routes, handlers, and setup functions |
| `Master Schema Audit.md` | Schema vs. code consistency audit (all 200 tabs) |
| `Database Backup Manager.md` | Backup system documentation |
| `Single File Application.md` | Single-sheet architecture feasibility analysis |
| `Master Git Ignore.md` | Git ignore rules for the repository |

---

## Build & Deployment

FlockOS source files in `FlockOS/Scripts/` and `FlockOS/Pages/` are the production files for the default deployment. Changes are live on push.

### Multi-Church Build System

Each church deployment is driven by a JSON config file in `FlockOS/Tools/Active Deployments/`. The build script copies the source tree and brands it per-church.

**Config schema** (`ChurchTemplate.json`):
```json
{
  "id": "",
  "name": "",
  "shortName": "",
  "brandName": "",
  "tagline": "Church Management & Ministry Platform",
  "logo": "",
  "themeColor": "#e8a838",
  "backgroundColor": "#1a1a2e",
  "databaseUrl": "",
  "adminEmail": "",
  "analyticsId": "",
  "version": "1.3"
}
```

**Build command:**
```bash
bash FlockOS/Tools/Development\ Scripts/build_churches.sh
```

**What it does:**
1. Reads each `.json` config in `Active Deployments/` (skips `ChurchTemplate.json`)
2. Copies source tree + root files into `Church/<shortName>/`
3. Replaces database URL, tagline, theme/background colors via `sed`
4. Replaces logo filename (only when config specifies one; empty = keep default)
5. Replaces `<title>`, `og:title` in **all** HTML files with church `name`
6. Replaces topbar brand text with `brandName`
7. Replaces offline page, lockdown pages, wall header/footer with church name + version
8. Updates `manifest.json` via `jq` (name, short_name, description, colors, icons)

**Active deployments:**

| Config | Name | Brand | Logo | URL |
|--------|------|-------|------|-----|
| `flockos-default.json` | FlockOS | Power to the People \| FlockOS | FlockOS_Midnight.png (default) | `Church/FlockOS/` |
| `flockos-tbc.json` | Trinity Baptist Church | Trinity Baptist Church \| FlockOS | FlockOS_Blue.png | `Church/TBC/` |
| `flockos-test.json` | Test FlockOS | Power to the People \| FlockOS | FlockOS_Midnight.png (default) | `Church/Test/` |

**Adding a new church:**
1. Copy `ChurchTemplate.json` → `flockos-newchurch.json`
2. Fill in all fields (see schema above)
3. Optionally add a logo image to `FlockOS/Images/`
4. Run `build_churches.sh`
5. Commit and push

### Development = Production

Source files in `FlockOS/Scripts/` and `FlockOS/Pages/` are the production files. All structural changes (folder renames, new modules) should be reflected in `manifest.json`, the root `index.html`, and the service worker (`the_living_water.js`). After any source change, re-run the build script to propagate to church deployments.

---

## Music Stand Module

The frontend Music Stand (`FlockOS/Scripts/the_shofar.js` — 1,621 lines) provides:
- Searchable song library with full CRUD
- Arrangement management with ChordPro chord notation
- Live Music Stand view — load a service plan, navigate songs with arrow keys
- PDF export — single arrangement or full setlist via jsPDF

Entry point: `window.openMusicStandApp()`

---

## License

**CRM Software** — Proprietary. Use, deployment, and modification require written permission from the author. Licensing available for churches and organizations upon request.

**Learning Modules** — Free for personal, church, and educational use.

See [LICENSE](LICENSE) for full terms.
